/**
 * @deprecated Use `@/lib/airportSearch` — kept for backward-compatible imports.
 */
export {
  searchAirports,
  getAirports,
  preloadAirports,
  getPopularAirports,
  findAirportByIata,
} from "@/lib/airportSearch";
